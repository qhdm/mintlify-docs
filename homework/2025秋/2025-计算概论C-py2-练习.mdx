## 题集链接
<Card
  title="OpenJudge/计算概论C(py)/2025-计算概论C-py2-练习"
  icon="code"
  href="http://jgcq.openjudge.cn/2025jgchw2lx/"
  arrow="true"
  horizontal
>

</Card>
## 01：基础输入输出练习-04

### 题目描述

输入为两行，第一行有一个浮点数，第二行有两个整数，输出三个数字的平均值，保留小数点后两位。

**输入**

输入为两行，第一行有一个浮点数，第二行有两个整数

**输出**

输出为一行，一个浮点数
### 题解
#### 思路
TODO
#### 知识点
<ResponseField name="输入函数与输出函数" type="第1章程序设计与Python导引">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：基础输入输出练习-04 lines icon="python"
    a = float(input())
    b, c = map(int, input().split())
    avg = (a + b + c) / 3
    print(f"{avg:.2f}")
    ```
    
  </Tab>
</Tabs>


## 02：基础输入输出练习-05

### 题目描述

给定一个整数、一个浮点数、一个字符串，以空格分开

输出整数除以7的余数，浮点数乘3（保留小数点后三位），以及字符串的最后一位，以空格分开

**输入**

输入为一行，分别为一个整数、一个浮点数、一个字符串，以空格分开

**输出**

输出为一行，分别为一个整数、一个浮点数、一个字符串，以空格分开

### 题解
#### 思路
TODO
#### 知识点
<ResponseField name="输入函数与输出函数" type="第1章程序设计与Python导引">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：基础输入输出练习-05 lines icon="python"
    a, b, s = input().split()
    print(f"{int(a)%7} {float(b)*3:.3f} {s[-1]}")
    ```
    
  </Tab>
</Tabs>

## 03：基础输入输出练习-07

### 题目描述

第一行读入一个字符串a（保证长度至少为6）

第二行读入一个字符串b（保证长度至少为6）

第一行取a的第6位输出

第二行取b的第4到6位输出

**输入**    
输入两行    
第一行读入一个字符串a（保证长度至少为6）  
第二行读入一个字符串b（保证长度至少为6）  

**输出**      
输出两行  
第一行取a的第6位输出  
第二行取b的第4到6位输出  

### 题解
#### 思路
TODO
#### 知识点
<ResponseField name="输入函数与输出函数" type="第1章程序设计与Python导引">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：基础输入输出练习-07 lines icon="python"
    str1 = input()
    str2 = input()
    print(str1[5])
    print(str2[3:6])
    ```
    
  </Tab>
</Tabs>

## 04：计算2的幂

### 题目描述
给定非负整数n，求2^n。

**输入**    
一个整数n。0 < = n < 31。

**输出**      
一个整数，即2的n次方。

### 题解
#### 思路
TODO
#### 知识点
<ResponseField name="数字类型-数据运算-算数运算符" type="第2章基本数据类型">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：基础输入输出练习-07 lines icon="python"
    n = int(input())
    print(2**n)
    ```
  </Tab>
</Tabs>

## 05：大象喝水

### 题目描述
一只大象口渴了，要喝20升水才能解渴，但现在只有一个深h厘米，底面半径为r厘米的小圆桶(h和r都是整数)。问大象至少要喝多少桶水才会解渴。

**输入**    
输入有一行：包行两个整数，以一个空格分开，分别表示小圆桶的深h和底面半径r，单位都是厘米。

**输出**      
输出一行，包含一个整数，表示大象至少要喝水的桶数。

### 题解
#### 思路
TODO
#### 知识点
<ResponseField name="数字类型-数据运算-算数运算符" type="第2章基本数据类型">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：大象喝水 lines icon="python"
    h, r = map(float, input().split())
    Pi = 3.14159
    V = Pi * r**2 * h
    num = 20 * 1000 / V
    # num 向上取整
    num = int(-(-num // 1))
    print(num)
    ```
  </Tab>
</Tabs>

## 06：数字转星期

### 题目描述
根据输入的数字1-7，输出对应的字符串Mon-Sun(均为3个字母的简写)

**输入**    
1~7范围内的一个数字

**输出**      
对应的星期的英文简写，首字母大写，后两个字母小写

### 题解
#### 思路
TODO
#### 知识点

#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：数字转星期 lines icon="python"
    weeks = ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"]
    day = int(input())
    print(weeks[day - 1])
    ```
  </Tab>
</Tabs>

## 07：分离整数的各个数位

### 题目描述
从键盘输入一个任意的三位整数，要求正确地分离出它的百位、十位和个位数，并分别在屏幕上输出，输出采用每行输出一个数的方式，不带其它符号。

**输入**    
一个任意的三位整数

**输出**      
一个任意的三位整数

### 题解
#### 思路
TODO
#### 知识点

#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：分离整数的各个数位 lines icon="python"
    nums = input()
    for num in nums:
        print(num)
    ```
  </Tab>
</Tabs>

## 08：苹果与虫子

### 题目描述
你买了一箱n个苹果，很不幸的是买完时箱子里混进了一条虫子。虫子每x小时能吃掉一个苹果，假设虫子在吃完一个苹果之前不会吃另一个，那么经过y小时你还有多少个苹果（包括还没有被虫子吃完的）？

**输入**    
输入仅一行，包括n，x和y（均为整数）。输入数据保证y < = n * x。

**输出**      
输出仅一行，剩下的苹果个数

### 题解
#### 思路
TODO
#### 知识点

#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：苹果与虫子 lines icon="python"
    n, x, y = map(int, input().split())
    print(n - y // x)
    ```
  </Tab>
</Tabs>

## 09：字符串交换

### 题目描述
输入两个长度为4的字符串，交换这两个字符串的前两个字符后输出

**输入**    
两个长度为4的字符串

**输出**      
交换这两个字符串的前两个字符后输出

### 题解
#### 思路
TODO
#### 知识点

#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：字符串交换 lines icon="python"
    str1 = input()
    str2 = input()
    str3 = str1[:2] + str2[2:]
    str4 = str2[:2] + str1[2:]
    print(str4)
    print(str3)
    ```
  </Tab>
</Tabs>

## 10：字符串大小写转换

### 题目描述
将一个字符串中的大写字母转换成小写字母，将小写字母转换成大写字母。

**输入**    
一行，一个字符串，一共有三个字符，而且第一个和第三个字符是小写字母，第二个字符是大写字母。

**输出**      
一行，转换后的字符串（还是三个字符，第一个和第三个是大写字母，第二个是小写字母）

### 题解
#### 思路
TODO
#### 知识点

#### 代码

<Tabs>
  <Tab title="尹思源">

    ```python 01：字符串大小写转换 lines icon="python"
    str1 = input()
    print(str1[0].upper() + str1[1].lower() + str1[2:].upper())
    ```
  </Tab>
</Tabs>
---
<div className="text-center text-xs text-gray-400 mt-10">
  贡献者：尹思源  
  上次修改：2025/10/23
</div>
---