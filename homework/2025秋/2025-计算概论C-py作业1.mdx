## 题集链接
<Card
  title="OpenJudge/计算概论C(py)/2025-计算概论C-py作业1"
  icon="code"
  href="http://jgcq.openjudge.cn/2025jgchw1/"
  arrow="true"
  horizontal
>
</Card>

## 01：两整数求和

### 题目描述

输入两个整数，求这两个整数之和

**输入**  
两行，每行一个整数

**输出**  
两个整数的和

**样例输入**
```
3
6
```

**样例输出**
```
9
```


### 题解
#### 思路
_待发布_
#### 知识点
<ResponseField name="输入函数与输出函数" type="第1章程序设计与Python导引">
</ResponseField>
<ResponseField name="数字类型-整型" type="第2章基本数据类型">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="陈旭峥">
    _待发布_
    
  </Tab>

  <Tab title="徐志衡">
    ```python 01：两整数求和 lines icon="python"
    a = int(input())
    b = int(input())
    c = a + b
    print(c)
    ```
  </Tab>
</Tabs>


## 02：简单的A*B问题

### 题目描述

输入两个数A和B，求A*B。


**输入**  
两行，第一行输入一个数A，第二行输入一个数B。A和B均为浮点数。

**输出**  
一个浮点数，即A*B的值。要求结果保留两位小数。

其中，商保留两位小数。

**样例输入**
```
1
3.4
```

**样例输出**
```
3.40
```


### 题解
#### 思路
_待发布_
#### 知识点
<ResponseField name="输入函数与输出函数" type="第1章程序设计与Python导引">
</ResponseField>
<ResponseField name="数字类型-浮点型" type="第2章基本数据类型">
</ResponseField>
<ResponseField name="字符串格式化" type="第2章基本数据类型">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="陈旭峥">

    _待发布_
    
  </Tab>

  <Tab title="徐志衡">
    ```python 02：简单的A*B问题 lines icon="python"
    a = float(input())
    b = float(input())
    c = a * b
    print(f'{c:.2f}')
    ```
  </Tab>
</Tabs>

## 03：计算三角形的周长和面积(基础版)

### 题目描述

输入三条边长，算出三角形的周长和面积，结果均保留两位小数。

说明：本题系统测试用的输入数据均可以构成三角形。


**输入**  
三行，每行一个整数，表示边长。

**输出**  
输出二行：<br />
第一行为周长； <br />
第二行为面积。

**样例输入**
```
3
4
5
```

**样例输出**
```
12.00
6.0
```


### 题解
#### 思路
_待发布_
#### 知识点
<ResponseField name="数字类型-数据运算-算数运算符" type="第2章基本数据类型">
</ResponseField>
<ResponseField name="字符串格式化" type="第2章基本数据类型">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="陈旭峥">

    _待发布_
    
  </Tab>

  <Tab title="徐志衡">
    ```python 03：计算三角形的周长和面积(基础版) lines icon="python"
    a = int(input())
    b = int(input())
    c = int(input())
    x = float(a + b + c)
    print(f'{x:.2f}')
    p = x / 2
    y = (p * (p - a) * (p - b) * (p - c)) ** 0.5
    print(round(y))
    ```
  </Tab>
</Tabs>

## 04：计算两点之间距离

### 题目描述

从键盘输入两点的坐标（x1,y1）(x2,y2)，计算两点之间的距离（结果保留两位小数）

**输入**  
4行，每行1个数(可以是小数)，分别是x1,y1,x2,y2

**输出**  
两点之间的距离（结果保留两位小数）

**样例输入**
```
0
0
3
5
```

**样例输出**
```
5.83
```


### 题解
#### 思路
_待发布_
#### 知识点
<ResponseField name="数字类型-数据运算-算数运算符" type="第2章基本数据类型">
</ResponseField>
<ResponseField name="字符串格式化" type="第2章基本数据类型">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="陈旭峥">

    _待发布_
    
  </Tab>

  <Tab title="徐志衡">
    ```python 04：计算两点之间距离 lines icon="python"
    x1 = float(input())
    x2 = float(input())
    y1 = float(input())
    y2 = float(input())
    d = ((x1 - y1) ** 2 + (x2 - y2) ** 2) ** 0.5
    print(f'{d:.2f}')
    ```
  </Tab>
</Tabs>

## 05：字符菱形

### 题目描述

给定一个字符，用它构造一个对角线长5个字符，倾斜放置的菱形。

**输入**  
输入只有一行， 包含一个字符。

**输出**  
该字符构成的菱形。

**样例输入**
```
*
```

**样例输出**
```
  *
 ***
*****
 ***
  *
```



### 题解
#### 思路
_待发布_
#### 知识点
<ResponseField name="字符串类型" type="第2章基本数据类型">
</ResponseField>
<ResponseField name="字符串格式化" type="第2章基本数据类型">
</ResponseField>
#### 代码

<Tabs>
  <Tab title="陈旭峥">

    _待发布_
    
  </Tab>

  <Tab title="徐志衡">
    ```python 05：字符菱形 lines icon="python"
    # 获取输入字符
    c = input()
    # 字符串的乘法：字符串乘以数字x表示将字符串重复x次，如'abc' * 3 = 'abcabcabc'
    """
    # 字符串的center方法：字符串center(width, fillchar)方法返回一个原字符串居中,
    并使用fillchar(默认空格)填充至长度 width 的新字符串
    """
    print(c.center(5, ' '))
    print((c * 3).center(5, ' '))
    print(c * 5)
    print((c * 3).center(5, ' '))
    print(c.center(5, ' '))
    ```
  </Tab>
</Tabs>

---
<div className="text-center text-xs text-gray-400 mt-10">
  贡献者：陈旭峥 徐志衡 
  上次修改：2025/10/09
</div>
---